---
const { headline, tekst, imgSrc } = Astro.props;
import img from "../assets/gruppeforloeb.png";
import { Image } from "astro:assets";
---

<article>
  <Image src={imgSrc} alt="Gruppe billede" width={700} height={1000} />
  <h3>{headline}</h3>
  <p>{tekst}</p>
</article>
